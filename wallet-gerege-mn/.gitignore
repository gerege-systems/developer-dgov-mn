# ── secrets / env ──
.env
*.env
!.env.example

# ── Go build / cache ──
bin/
tmp/
.cache/
coverage.out
coverage.html
*.log
# cmd/* binaries built ad-hoc at repo root
/api
/worker

# ── OS / editor ──
.DS_Store
.idea/
.vscode/
